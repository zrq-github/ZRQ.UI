<ResourceDictionary xmlns = "http://schemas.microsoft.com/winfx/2006/xaml/presentation"
                    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
                    xmlns:local="clr-namespace:ZRQ.UI">

    <Style x:Key="HighlightLabel" TargetType = "{x:Type local:HighlightLabel}">
        <Setter Property="Template">
            <Setter.Value>
                <ControlTemplate TargetType = "{x:Type local:HighlightLabel}" >
                    <TextBlock x:Name="PART_TextBlock" TextWrapping="Wrap"/>
                </ControlTemplate>
            </Setter.Value>
        </Setter>
    </Style>
    
</ResourceDictionary>

 
