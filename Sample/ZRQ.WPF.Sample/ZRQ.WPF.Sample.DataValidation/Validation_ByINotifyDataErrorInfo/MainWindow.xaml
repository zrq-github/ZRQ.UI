<Window x:Class="Validation_ByINotifyDataErrorInfo.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:Validation_ByINotifyDataErrorInfo"
        mc:Ignorable="d"
        Title="MainWindow" Height="450" Width="800">
    <Window.DataContext>
        <local:ValidationModel />
    </Window.DataContext>
    <Grid Margin="100">
        <StackPanel>
            <Label>Username</Label>
            <TextBox Text="{Binding Username, ValidatesOnNotifyDataErrors=True,
                UpdateSourceTrigger=PropertyChanged}" />
            <TextBox Text="{Binding Email,UpdateSourceTrigger=PropertyChanged,ValidatesOnNotifyDataErrors=True}"></TextBox>
            <TextBox Text="{Binding RepeatEmail,UpdateSourceTrigger=PropertyChanged, ValidatesOnNotifyDataErrors=True}"></TextBox>
            <Button>Submit</Button>
        </StackPanel>
    </Grid>
</Window>
