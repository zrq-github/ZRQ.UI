<Window x:Class="Validation_ByDataErrorInfo.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:Validation_ByDataErrorInfo"
        mc:Ignorable="d"
        Title="MainWindow" Height="450" Width="800">
    <Window.DataContext>
        <local:ValidationModel />
    </Window.DataContext>
    <Grid Margin="100">
        <StackPanel>
            <Label>Username</Label>
            <TextBox 
                x:Name="tb_Username"
                Text="{Binding Username, ValidatesOnDataErrors=True,
                UpdateSourceTrigger=PropertyChanged}"
                     ToolTip="{Binding ErrorCollection[Username]}" />
            <Button Click="Button_Click">Submit</Button>
            <TextBox Text="{Binding Phone,ValidatesOnDataErrors=True,UpdateSourceTrigger=PropertyChanged}"
                     ToolTip="{Binding ErrorCollection[Username]}"></TextBox>
        </StackPanel>
    </Grid>
</Window>
